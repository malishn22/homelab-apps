/** Interval for polling server status and logs (ms) */
export const POLL_INTERVAL_MS = 5000;

/** Maximum log entries kept in memory per server */
export const MAX_LOG_ENTRIES = 400;

/** Duration to show "Saved" feedback (ms) */
export const SAVED_MESSAGE_DURATION = 2000;

/** Default RAM allocation for new servers (MB) */
export const DEFAULT_RAM_MB = 4096;

/** Default port for new Minecraft servers */
export const DEFAULT_PORT = 25565;

/** Debounce delay for search and other inputs (ms) */
export const DEBOUNCE_MS = 500;

/** Auto-scroll cooldown for the console (ms) */
export const AUTO_SCROLL_COOLDOWN_MS = 900;
